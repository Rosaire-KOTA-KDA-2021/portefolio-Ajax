/* -----------------------------------------------
/* Author : Vincent Garreau  - vincentgarreau.com
/* MIT license: http://opensource.org/licenses/MIT
/* Demo / Generator : vincentgarreau.com/particles.js
/* GitHub : github.com/VincentGarreau/particles.js
/* How to use? : Check the GitHub README
/* v2.0.0
/* ----------------------------------------------- */

let particuleJs = function (tag_id, params) {
  let canvasElements = document.querySelector(
    "#" + tag_id + " > .particles-js-canvas-el"
  );

  /* particles.js letiables with default values */
  this.particuleJs = {
    canvas: {
      el: canvasElements,
      w: canvasElements.offsetWidth,
      h: canvasElements.offsetHeight,
    },
    particles: {
      number: {
        value: 400,
        density: {
          enable: true,
          value_area: 800,
        },
      },
      color: {
        value: "#fff",
      },
      shape: {
        type: "circle",
        stroke: {
          width: 0,
          color: "#ff0000",
        },
        polygon: {
          nb_sides: 5,
        },
        image: {
          src: "",
          width: 100,
          height: 100,
        },
      },
      opacity: {
        value: 1,
        random: false,
        anim: {
          enable: false,
          speed: 2,
          opacity_min: 0,
          sync: false,
        },
      },
      size: {
        value: 20,
        random: false,
        anim: {
          enable: false,
          speed: 20,
          size_min: 0,
          sync: false,
        },
      },
      line_linked: {
        enable: true,
        distance: 100,
        color: "#fff",
        opacity: 1,
        width: 1,
      },
      move: {
        enable: true,
        speed: 2,
        direction: "none",
        random: false,
        straight: false,
        out_mode: "out",
        bounce: false,
        attract: {
          enable: false,
          rotateX: 3000,
          rotateY: 3000,
        },
      },
      array: [],
    },
    interactivity: {
      detect_on: "canvas",
      events: {
        onhover: {
          enable: true,
          mode: "grab",
        },
        onclick: {
          enable: true,
          mode: "push",
        },
        resize: true,
      },
      modes: {
        grab: {
          distance: 100,
          line_linked: {
            opacity: 1,
          },
        },
        bubble: {
          distance: 200,
          size: 80,
          duration: 0.4,
        },
        repulse: {
          distance: 200,
          duration: 0.4,
        },
        push: {
          particles_nb: 4,
        },
        remove: {
          particles_nb: 2,
        },
      },
      mouse: {},
    },
    retina_detect: false,
    fn: {
      interact: {},
      modes: {},
      vendors: {},
    },
    tmp: {},
  };

  let particuleJs = this.particuleJs;

  /* params settings */
  if (params) {
    Object.deepExtend(particuleJs, params);
  }

  particuleJs.tmp.obj = {
    size_value: particuleJs.particles.size.value,
    size_anim_speed: particuleJs.particles.size.anim.speed,
    move_speed: particuleJs.particles.move.speed,
    line_linked_distance: particuleJs.particles.line_linked.distance,
    line_linked_width: particuleJs.particles.line_linked.width,
    mode_grab_distance: particuleJs.interactivity.modes.grab.distance,
    mode_bubble_distance: particuleJs.interactivity.modes.bubble.distance,
    mode_bubble_size: particuleJs.interactivity.modes.bubble.size,
    mode_repulse_distance: particuleJs.interactivity.modes.repulse.distance,
  };

  particuleJs.fn.retinaInit = function () {
    if (particuleJs.retina_detect && window.devicePixelRatio > 1) {
      particuleJs.canvas.pxratio = window.devicePixelRatio;
      particuleJs.tmp.retina = true;
    } else {
      particuleJs.canvas.pxratio = 1;
      particuleJs.tmp.retina = false;
    }

    particuleJs.canvas.w =
      particuleJs.canvas.el.offsetWidth * particuleJs.canvas.pxratio;
    particuleJs.canvas.h =
      particuleJs.canvas.el.offsetHeight * particuleJs.canvas.pxratio;

    particuleJs.particles.size.value =
      particuleJs.tmp.obj.size_value * particuleJs.canvas.pxratio;
    particuleJs.particles.size.anim.speed =
      particuleJs.tmp.obj.size_anim_speed * particuleJs.canvas.pxratio;
    particuleJs.particles.move.speed =
      particuleJs.tmp.obj.move_speed * particuleJs.canvas.pxratio;
    particuleJs.particles.line_linked.distance =
      particuleJs.tmp.obj.line_linked_distance * particuleJs.canvas.pxratio;
    particuleJs.interactivity.modes.grab.distance =
      particuleJs.tmp.obj.mode_grab_distance * particuleJs.canvas.pxratio;
    particuleJs.interactivity.modes.bubble.distance =
      particuleJs.tmp.obj.mode_bubble_distance * particuleJs.canvas.pxratio;
    particuleJs.particles.line_linked.width =
      particuleJs.tmp.obj.line_linked_width * particuleJs.canvas.pxratio;
    particuleJs.interactivity.modes.bubble.size =
      particuleJs.tmp.obj.mode_bubble_size * particuleJs.canvas.pxratio;
    particuleJs.interactivity.modes.repulse.distance =
      particuleJs.tmp.obj.mode_repulse_distance * particuleJs.canvas.pxratio;
  };

  /* ---------- particuleJs functions - canvas ------------ */

  particuleJs.fn.canvasInit = function () {
    particuleJs.canvas.ctx = particuleJs.canvas.el.getContext("2d");
  };

  particuleJs.fn.canvasSize = function () {
    particuleJs.canvas.el.width = particuleJs.canvas.w;
    particuleJs.canvas.el.height = particuleJs.canvas.h;

    if (particuleJs && particuleJs.interactivity.events.resize) {
      window.addEventListener("resize", function () {
        particuleJs.canvas.w = particuleJs.canvas.el.offsetWidth;
        particuleJs.canvas.h = particuleJs.canvas.el.offsetHeight;

        /* resize canvas */
        if (particuleJs.tmp.retina) {
          particuleJs.canvas.w *= particuleJs.canvas.pxratio;
          particuleJs.canvas.h *= particuleJs.canvas.pxratio;
        }

        particuleJs.canvas.el.width = particuleJs.canvas.w;
        particuleJs.canvas.el.height = particuleJs.canvas.h;

        /* repaint canvas on anim disabled */
        if (!particuleJs.particles.move.enable) {
          particuleJs.fn.particlesEmpty();
          particuleJs.fn.particlesCreate();
          particuleJs.fn.particlesDraw();
          particuleJs.fn.vendors.densityAutoParticles();
        }

        /* density particles enabled */
        particuleJs.fn.vendors.densityAutoParticles();
      });
    }
  };

  particuleJs.fn.canvasPaint = function () {
    particuleJs.canvas.ctx.fillRect(
      0,
      0,
      particuleJs.canvas.w,
      particuleJs.canvas.h
    );
  };

  particuleJs.fn.canvasClear = function () {
    particuleJs.canvas.ctx.clearRect(
      0,
      0,
      particuleJs.canvas.w,
      particuleJs.canvas.h
    );
  };

  /* --------- particuleJs functions - particles ----------- */

  particuleJs.fn.particle = function (color, opacity, position) {
    /* size */
    this.radius =
      (particuleJs.particles.size.random ? Math.random() : 1) *
      particuleJs.particles.size.value;
    if (particuleJs.particles.size.anim.enable) {
      this.size_status = false;
      this.vs = particuleJs.particles.size.anim.speed / 100;
      if (!particuleJs.particles.size.anim.sync) {
        this.vs = this.vs * Math.random();
      }
    }

    /* position */
    this.x = position ? position.x : Math.random() * particuleJs.canvas.w;
    this.y = position ? position.y : Math.random() * particuleJs.canvas.h;

    /* check position  - into the canvas */
    if (this.x > particuleJs.canvas.w - this.radius * 2)
      this.x = this.x - this.radius;
    else if (this.x < this.radius * 2) this.x = this.x + this.radius;
    if (this.y > particuleJs.canvas.h - this.radius * 2)
      this.y = this.y - this.radius;
    else if (this.y < this.radius * 2) this.y = this.y + this.radius;

    /* check position - avoid overlap */
    if (particuleJs.particles.move.bounce) {
      particuleJs.fn.vendors.checkOverlap(this, position);
    }

    /* color */
    this.color = {};
    if (typeof color.value == "object") {
      if (color.value instanceof Array) {
        let color_selected =
          color.value[
            Math.floor(Math.random() * particuleJs.particles.color.value.length)
          ];
        this.color.rgb = hexToRgb(color_selected);
      } else {
        if (
          color.value.r != undefined &&
          color.value.g != undefined &&
          color.value.b != undefined
        ) {
          this.color.rgb = {
            r: color.value.r,
            g: color.value.g,
            b: color.value.b,
          };
        }
        if (
          color.value.h != undefined &&
          color.value.s != undefined &&
          color.value.l != undefined
        ) {
          this.color.hsl = {
            h: color.value.h,
            s: color.value.s,
            l: color.value.l,
          };
        }
      }
    } else if (color.value == "random") {
      this.color.rgb = {
        r: Math.floor(Math.random() * (255 - 0 + 1)) + 0,
        g: Math.floor(Math.random() * (255 - 0 + 1)) + 0,
        b: Math.floor(Math.random() * (255 - 0 + 1)) + 0,
      };
    } else if (typeof color.value == "string") {
      this.color = color;
      this.color.rgb = hexToRgb(this.color.value);
    }

    /* opacity */
    this.opacity =
      (particuleJs.particles.opacity.random ? Math.random() : 1) *
      particuleJs.particles.opacity.value;
    if (particuleJs.particles.opacity.anim.enable) {
      this.opacity_status = false;
      this.vo = particuleJs.particles.opacity.anim.speed / 100;
      if (!particuleJs.particles.opacity.anim.sync) {
        this.vo = this.vo * Math.random();
      }
    }

    /* animation - velocity for speed */
    let velbase = {};
    switch (particuleJs.particles.move.direction) {
      case "top":
        velbase = { x: 0, y: -1 };
        break;
      case "top-right":
        velbase = { x: 0.5, y: -0.5 };
        break;
      case "right":
        velbase = { x: 1, y: -0 };
        break;
      case "bottom-right":
        velbase = { x: 0.5, y: 0.5 };
        break;
      case "bottom":
        velbase = { x: 0, y: 1 };
        break;
      case "bottom-left":
        velbase = { x: -0.5, y: 1 };
        break;
      case "left":
        velbase = { x: -1, y: 0 };
        break;
      case "top-left":
        velbase = { x: -0.5, y: -0.5 };
        break;
      default:
        velbase = { x: 0, y: 0 };
        break;
    }

    if (particuleJs.particles.move.straight) {
      this.vx = velbase.x;
      this.vy = velbase.y;
      if (particuleJs.particles.move.random) {
        this.vx = this.vx * Math.random();
        this.vy = this.vy * Math.random();
      }
    } else {
      this.vx = velbase.x + Math.random() - 0.5;
      this.vy = velbase.y + Math.random() - 0.5;
    }

    // let theta = 2.0 * Math.PI * Math.random();
    // this.vx = Math.cos(theta);
    // this.vy = Math.sin(theta);

    this.vx_i = this.vx;
    this.vy_i = this.vy;

    /* if shape is image */

    let shape_type = particuleJs.particles.shape.type;
    if (typeof shape_type == "object") {
      if (shape_type instanceof Array) {
        let shape_selected =
          shape_type[Math.floor(Math.random() * shape_type.length)];
        this.shape = shape_selected;
      }
    } else {
      this.shape = shape_type;
    }

    if (this.shape == "image") {
      let sh = particuleJs.particles.shape;
      this.img = {
        src: sh.image.src,
        ratio: sh.image.width / sh.image.height,
      };
      if (!this.img.ratio) this.img.ratio = 1;
      if (
        particuleJs.tmp.img_type == "svg" &&
        particuleJs.tmp.source_svg != undefined
      ) {
        particuleJs.fn.vendors.createSvgImg(this);
        if (particuleJs.tmp.pushing) {
          this.img.loaded = false;
        }
      }
    }
  };

  particuleJs.fn.particle.prototype.draw = function () {
    let p = this;

    if (p.radius_bubble != undefined) {
      let radius = p.radius_bubble;
    } else {
      let radius = p.radius;
    }

    if (p.opacity_bubble != undefined) {
      let opacity = p.opacity_bubble;
    } else {
      let opacity = p.opacity;
    }

    if (p.color.rgb) {
      let color_value =
        "rgba(" +
        p.color.rgb.r +
        "," +
        p.color.rgb.g +
        "," +
        p.color.rgb.b +
        "," +
        opacity +
        ")";
    } else {
      let color_value =
        "hsla(" +
        p.color.hsl.h +
        "," +
        p.color.hsl.s +
        "%," +
        p.color.hsl.l +
        "%," +
        opacity +
        ")";
    }

    particuleJs.canvas.ctx.fillStyle = color_value;
    particuleJs.canvas.ctx.beginPath();

    switch (p.shape) {
      case "circle":
        particuleJs.canvas.ctx.arc(p.x, p.y, radius, 0, Math.PI * 2, false);
        break;

      case "edge":
        particuleJs.canvas.ctx.rect(
          p.x - radius,
          p.y - radius,
          radius * 2,
          radius * 2
        );
        break;

      case "triangle":
        particuleJs.fn.vendors.drawShape(
          particuleJs.canvas.ctx,
          p.x - radius,
          p.y + radius / 1.66,
          radius * 2,
          3,
          2
        );
        break;

      case "polygon":
        particuleJs.fn.vendors.drawShape(
          particuleJs.canvas.ctx,
          p.x - radius / (particuleJs.particles.shape.polygon.nb_sides / 3.5), // startX
          p.y - radius / (2.66 / 3.5), // startY
          (radius * 2.66) / (particuleJs.particles.shape.polygon.nb_sides / 3), // sideLength
          particuleJs.particles.shape.polygon.nb_sides, // sideCountNumerator
          1 // sideCountDenominator
        );
        break;

      case "star":
        particuleJs.fn.vendors.drawShape(
          particuleJs.canvas.ctx,
          p.x -
            (radius * 2) / (particuleJs.particles.shape.polygon.nb_sides / 4), // startX
          p.y - radius / ((2 * 2.66) / 3.5), // startY
          (radius * 2 * 2.66) /
            (particuleJs.particles.shape.polygon.nb_sides / 3), // sideLength
          particuleJs.particles.shape.polygon.nb_sides, // sideCountNumerator
          2 // sideCountDenominator
        );
        break;

      case "image":
        function draw() {
          particuleJs.canvas.ctx.drawImage(
            img_obj,
            p.x - radius,
            p.y - radius,
            radius * 2,
            (radius * 2) / p.img.ratio
          );
        }

        if (particuleJs.tmp.img_type == "svg") {
          let img_obj = p.img.obj;
        } else {
          let img_obj = particuleJs.tmp.img_obj;
        }

        if (img_obj) {
          draw();
        }

        break;
    }

    particuleJs.canvas.ctx.closePath();

    if (particuleJs.particles.shape.stroke.width > 0) {
      particuleJs.canvas.ctx.strokeStyle =
        particuleJs.particles.shape.stroke.color;
      particuleJs.canvas.ctx.lineWidth =
        particuleJs.particles.shape.stroke.width;
      particuleJs.canvas.ctx.stroke();
    }

    particuleJs.canvas.ctx.fill();
  };

  particuleJs.fn.particlesCreate = function () {
    for (let i = 0; i < particuleJs.particles.number.value; i++) {
      particuleJs.particles.array.push(
        new particuleJs.fn.particle(
          particuleJs.particles.color,
          particuleJs.particles.opacity.value
        )
      );
    }
  };

  particuleJs.fn.particlesUpdate = function () {
    for (let i = 0; i < particuleJs.particles.array.length; i++) {
      /* the particle */
      let p = particuleJs.particles.array[i];

      // let d = ( dx = particuleJs.interactivity.mouse.click_pos_x - p.x ) * dx + ( dy = particuleJs.interactivity.mouse.click_pos_y - p.y ) * dy;
      // let f = -BANG_SIZE / d;
      // if ( d < BANG_SIZE ) {
      //     let t = Math.atan2( dy, dx );
      //     p.vx = f * Math.cos(t);
      //     p.vy = f * Math.sin(t);
      // }

      /* move the particle */
      if (particuleJs.particles.move.enable) {
        let ms = particuleJs.particles.move.speed / 2;
        p.x += p.vx * ms;
        p.y += p.vy * ms;
      }

      /* change opacity status */
      if (particuleJs.particles.opacity.anim.enable) {
        if (p.opacity_status == true) {
          if (p.opacity >= particuleJs.particles.opacity.value)
            p.opacity_status = false;
          p.opacity += p.vo;
        } else {
          if (p.opacity <= particuleJs.particles.opacity.anim.opacity_min)
            p.opacity_status = true;
          p.opacity -= p.vo;
        }
        if (p.opacity < 0) p.opacity = 0;
      }

      /* change size */
      if (particuleJs.particles.size.anim.enable) {
        if (p.size_status == true) {
          if (p.radius >= particuleJs.particles.size.value)
            p.size_status = false;
          p.radius += p.vs;
        } else {
          if (p.radius <= particuleJs.particles.size.anim.size_min)
            p.size_status = true;
          p.radius -= p.vs;
        }
        if (p.radius < 0) p.radius = 0;
      }

      /* change particle position if it is out of canvas */
      if (particuleJs.particles.move.out_mode == "bounce") {
        let new_pos = {
          x_left: p.radius,
          x_right: particuleJs.canvas.w,
          y_top: p.radius,
          y_bottom: particuleJs.canvas.h,
        };
      } else {
        let new_pos = {
          x_left: -p.radius,
          x_right: particuleJs.canvas.w + p.radius,
          y_top: -p.radius,
          y_bottom: particuleJs.canvas.h + p.radius,
        };
      }

      if (p.x - p.radius > particuleJs.canvas.w) {
        p.x = new_pos.x_left;
        p.y = Math.random() * particuleJs.canvas.h;
      } else if (p.x + p.radius < 0) {
        p.x = new_pos.x_right;
        p.y = Math.random() * particuleJs.canvas.h;
      }
      if (p.y - p.radius > particuleJs.canvas.h) {
        p.y = new_pos.y_top;
        p.x = Math.random() * particuleJs.canvas.w;
      } else if (p.y + p.radius < 0) {
        p.y = new_pos.y_bottom;
        p.x = Math.random() * particuleJs.canvas.w;
      }

      /* out of canvas modes */
      switch (particuleJs.particles.move.out_mode) {
        case "bounce":
          if (p.x + p.radius > particuleJs.canvas.w) p.vx = -p.vx;
          else if (p.x - p.radius < 0) p.vx = -p.vx;
          if (p.y + p.radius > particuleJs.canvas.h) p.vy = -p.vy;
          else if (p.y - p.radius < 0) p.vy = -p.vy;
          break;
      }

      /* events */
      if (isInArray("grab", particuleJs.interactivity.events.onhover.mode)) {
        particuleJs.fn.modes.grabParticle(p);
      }

      if (
        isInArray("bubble", particuleJs.interactivity.events.onhover.mode) ||
        isInArray("bubble", particuleJs.interactivity.events.onclick.mode)
      ) {
        particuleJs.fn.modes.bubbleParticle(p);
      }

      if (
        isInArray("repulse", particuleJs.interactivity.events.onhover.mode) ||
        isInArray("repulse", particuleJs.interactivity.events.onclick.mode)
      ) {
        particuleJs.fn.modes.repulseParticle(p);
      }

      /* interaction auto between particles */
      if (
        particuleJs.particles.line_linked.enable ||
        particuleJs.particles.move.attract.enable
      ) {
        for (let j = i + 1; j < particuleJs.particles.array.length; j++) {
          let p2 = particuleJs.particles.array[j];

          /* link particles */
          if (particuleJs.particles.line_linked.enable) {
            particuleJs.fn.interact.linkParticles(p, p2);
          }

          /* attract particles */
          if (particuleJs.particles.move.attract.enable) {
            particuleJs.fn.interact.attractParticles(p, p2);
          }

          /* bounce particles */
          if (particuleJs.particles.move.bounce) {
            particuleJs.fn.interact.bounceParticles(p, p2);
          }
        }
      }
    }
  };

  particuleJs.fn.particlesDraw = function () {
    /* clear canvas */
    particuleJs.canvas.ctx.clearRect(
      0,
      0,
      particuleJs.canvas.w,
      particuleJs.canvas.h
    );

    /* update each particles param */
    particuleJs.fn.particlesUpdate();

    /* draw each particle */
    for (let i = 0; i < particuleJs.particles.array.length; i++) {
      let p = particuleJs.particles.array[i];
      p.draw();
    }
  };

  particuleJs.fn.particlesEmpty = function () {
    particuleJs.particles.array = [];
  };

  particuleJs.fn.particlesRefresh = function () {
    /* init all */
    cancelRequestAnimFrame(particuleJs.fn.checkAnimFrame);
    cancelRequestAnimFrame(particuleJs.fn.drawAnimFrame);
    particuleJs.tmp.source_svg = undefined;
    particuleJs.tmp.img_obj = undefined;
    particuleJs.tmp.count_svg = 0;
    particuleJs.fn.particlesEmpty();
    particuleJs.fn.canvasClear();

    /* restart */
    particuleJs.fn.vendors.start();
  };

  /* ---------- particuleJs functions - particles interaction ------------ */

  particuleJs.fn.interact.linkParticles = function (p1, p2) {
    let dx = p1.x - p2.x,
      dy = p1.y - p2.y,
      dist = Math.sqrt(dx * dx + dy * dy);

    /* draw a line between p1 and p2 if the distance between them is under the config distance */
    if (dist <= particuleJs.particles.line_linked.distance) {
      let opacity_line =
        particuleJs.particles.line_linked.opacity -
        dist /
          (1 / particuleJs.particles.line_linked.opacity) /
          particuleJs.particles.line_linked.distance;

      if (opacity_line > 0) {
        /* style */
        let color_line = particuleJs.particles.line_linked.color_rgb_line;
        particuleJs.canvas.ctx.strokeStyle =
          "rgba(" +
          color_line.r +
          "," +
          color_line.g +
          "," +
          color_line.b +
          "," +
          opacity_line +
          ")";
        particuleJs.canvas.ctx.lineWidth =
          particuleJs.particles.line_linked.width;
        //particuleJs.canvas.ctx.lineCap = 'round'; /* performance issue */

        /* path */
        particuleJs.canvas.ctx.beginPath();
        particuleJs.canvas.ctx.moveTo(p1.x, p1.y);
        particuleJs.canvas.ctx.lineTo(p2.x, p2.y);
        particuleJs.canvas.ctx.stroke();
        particuleJs.canvas.ctx.closePath();
      }
    }
  };

  particuleJs.fn.interact.attractParticles = function (p1, p2) {
    /* condensed particles */
    let dx = p1.x - p2.x,
      dy = p1.y - p2.y,
      dist = Math.sqrt(dx * dx + dy * dy);

    if (dist <= particuleJs.particles.line_linked.distance) {
      let ax = dx / (particuleJs.particles.move.attract.rotateX * 1000),
        ay = dy / (particuleJs.particles.move.attract.rotateY * 1000);

      p1.vx -= ax;
      p1.vy -= ay;

      p2.vx += ax;
      p2.vy += ay;
    }
  };

  particuleJs.fn.interact.bounceParticles = function (p1, p2) {
    let dx = p1.x - p2.x,
      dy = p1.y - p2.y,
      dist = Math.sqrt(dx * dx + dy * dy),
      dist_p = p1.radius + p2.radius;

    if (dist <= dist_p) {
      p1.vx = -p1.vx;
      p1.vy = -p1.vy;

      p2.vx = -p2.vx;
      p2.vy = -p2.vy;
    }
  };

  /* ---------- particuleJs functions - modes events ------------ */

  particuleJs.fn.modes.pushParticles = function (nb, pos) {
    particuleJs.tmp.pushing = true;

    for (let i = 0; i < nb; i++) {
      particuleJs.particles.array.push(
        new particuleJs.fn.particle(
          particuleJs.particles.color,
          particuleJs.particles.opacity.value,
          {
            x: pos ? pos.pos_x : Math.random() * particuleJs.canvas.w,
            y: pos ? pos.pos_y : Math.random() * particuleJs.canvas.h,
          }
        )
      );
      if (i == nb - 1) {
        if (!particuleJs.particles.move.enable) {
          particuleJs.fn.particlesDraw();
        }
        particuleJs.tmp.pushing = false;
      }
    }
  };

  particuleJs.fn.modes.removeParticles = function (nb) {
    particuleJs.particles.array.splice(0, nb);
    if (!particuleJs.particles.move.enable) {
      particuleJs.fn.particlesDraw();
    }
  };

  particuleJs.fn.modes.bubbleParticle = function (p) {
    /* on hover event */
    if (
      particuleJs.interactivity.events.onhover.enable &&
      isInArray("bubble", particuleJs.interactivity.events.onhover.mode)
    ) {
      let dx_mouse = p.x - particuleJs.interactivity.mouse.pos_x,
        dy_mouse = p.y - particuleJs.interactivity.mouse.pos_y,
        dist_mouse = Math.sqrt(dx_mouse * dx_mouse + dy_mouse * dy_mouse),
        ratio =
          1 - dist_mouse / particuleJs.interactivity.modes.bubble.distance;

      function init() {
        p.opacity_bubble = p.opacity;
        p.radius_bubble = p.radius;
      }

      /* mousemove - check ratio */
      if (dist_mouse <= particuleJs.interactivity.modes.bubble.distance) {
        if (ratio >= 0 && particuleJs.interactivity.status == "mousemove") {
          /* size */
          if (
            particuleJs.interactivity.modes.bubble.size !=
            particuleJs.particles.size.value
          ) {
            if (
              particuleJs.interactivity.modes.bubble.size >
              particuleJs.particles.size.value
            ) {
              let size =
                p.radius + particuleJs.interactivity.modes.bubble.size * ratio;
              if (size >= 0) {
                p.radius_bubble = size;
              }
            } else {
              let dif = p.radius - particuleJs.interactivity.modes.bubble.size,
                size = p.radius - dif * ratio;
              if (size > 0) {
                p.radius_bubble = size;
              } else {
                p.radius_bubble = 0;
              }
            }
          }

          /* opacity */
          if (
            particuleJs.interactivity.modes.bubble.opacity !=
            particuleJs.particles.opacity.value
          ) {
            if (
              particuleJs.interactivity.modes.bubble.opacity >
              particuleJs.particles.opacity.value
            ) {
              let opacity =
                particuleJs.interactivity.modes.bubble.opacity * ratio;
              if (
                opacity > p.opacity &&
                opacity <= particuleJs.interactivity.modes.bubble.opacity
              ) {
                p.opacity_bubble = opacity;
              }
            } else {
              let opacity =
                p.opacity -
                (particuleJs.particles.opacity.value -
                  particuleJs.interactivity.modes.bubble.opacity) *
                  ratio;
              if (
                opacity < p.opacity &&
                opacity >= particuleJs.interactivity.modes.bubble.opacity
              ) {
                p.opacity_bubble = opacity;
              }
            }
          }
        }
      } else {
        init();
      }

      /* mouseleave */
      if (particuleJs.interactivity.status == "mouseleave") {
        init();
      }
    } else if (
      /* on click event */
      particuleJs.interactivity.events.onclick.enable &&
      isInArray("bubble", particuleJs.interactivity.events.onclick.mode)
    ) {
      if (particuleJs.tmp.bubble_clicking) {
        let dx_mouse = p.x - particuleJs.interactivity.mouse.click_pos_x,
          dy_mouse = p.y - particuleJs.interactivity.mouse.click_pos_y,
          dist_mouse = Math.sqrt(dx_mouse * dx_mouse + dy_mouse * dy_mouse),
          time_spent =
            (new Date().getTime() -
              particuleJs.interactivity.mouse.click_time) /
            1000;

        if (time_spent > particuleJs.interactivity.modes.bubble.duration) {
          particuleJs.tmp.bubble_duration_end = true;
        }

        if (time_spent > particuleJs.interactivity.modes.bubble.duration * 2) {
          particuleJs.tmp.bubble_clicking = false;
          particuleJs.tmp.bubble_duration_end = false;
        }
      }

      function process(bubble_param, particles_param, p_obj_bubble, p_obj, id) {
        if (bubble_param != particles_param) {
          if (!particuleJs.tmp.bubble_duration_end) {
            if (dist_mouse <= particuleJs.interactivity.modes.bubble.distance) {
              if (p_obj_bubble != undefined) let obj = p_obj_bubble;
              else let obj = p_obj;
              if (obj != bubble_param) {
                let value =
                  p_obj -
                  (time_spent * (p_obj - bubble_param)) /
                    particuleJs.interactivity.modes.bubble.duration;
                if (id == "size") p.radius_bubble = value;
                if (id == "opacity") p.opacity_bubble = value;
              }
            } else {
              if (id == "size") p.radius_bubble = undefined;
              if (id == "opacity") p.opacity_bubble = undefined;
            }
          } else {
            if (p_obj_bubble != undefined) {
              let value_tmp =
                  p_obj -
                  (time_spent * (p_obj - bubble_param)) /
                    particuleJs.interactivity.modes.bubble.duration,
                dif = bubble_param - value_tmp;
              value = bubble_param + dif;
              if (id == "size") p.radius_bubble = value;
              if (id == "opacity") p.opacity_bubble = value;
            }
          }
        }
      }

      if (particuleJs.tmp.bubble_clicking) {
        /* size */
        process(
          particuleJs.interactivity.modes.bubble.size,
          particuleJs.particles.size.value,
          p.radius_bubble,
          p.radius,
          "size"
        );
        /* opacity */
        process(
          particuleJs.interactivity.modes.bubble.opacity,
          particuleJs.particles.opacity.value,
          p.opacity_bubble,
          p.opacity,
          "opacity"
        );
      }
    }
  };

  particuleJs.fn.modes.repulseParticle = function (p) {
    if (
      particuleJs.interactivity.events.onhover.enable &&
      isInArray("repulse", particuleJs.interactivity.events.onhover.mode) &&
      particuleJs.interactivity.status == "mousemove"
    ) {
      let dx_mouse = p.x - particuleJs.interactivity.mouse.pos_x,
        dy_mouse = p.y - particuleJs.interactivity.mouse.pos_y,
        dist_mouse = Math.sqrt(dx_mouse * dx_mouse + dy_mouse * dy_mouse);

      let normVec = { x: dx_mouse / dist_mouse, y: dy_mouse / dist_mouse },
        repulseRadius = particuleJs.interactivity.modes.repulse.distance,
        velocity = 100,
        repulseFactor = clamp(
          (1 / repulseRadius) *
            (-1 * Math.pow(dist_mouse / repulseRadius, 2) + 1) *
            repulseRadius *
            velocity,
          0,
          50
        );

      let pos = {
        x: p.x + normVec.x * repulseFactor,
        y: p.y + normVec.y * repulseFactor,
      };

      if (particuleJs.particles.move.out_mode == "bounce") {
        if (pos.x - p.radius > 0 && pos.x + p.radius < particuleJs.canvas.w)
          p.x = pos.x;
        if (pos.y - p.radius > 0 && pos.y + p.radius < particuleJs.canvas.h)
          p.y = pos.y;
      } else {
        p.x = pos.x;
        p.y = pos.y;
      }
    } else if (
      particuleJs.interactivity.events.onclick.enable &&
      isInArray("repulse", particuleJs.interactivity.events.onclick.mode)
    ) {
      if (!particuleJs.tmp.repulse_finish) {
        particuleJs.tmp.repulse_count++;
        if (
          particuleJs.tmp.repulse_count == particuleJs.particles.array.length
        ) {
          particuleJs.tmp.repulse_finish = true;
        }
      }

      if (particuleJs.tmp.repulse_clicking) {
        let repulseRadius = Math.pow(
          particuleJs.interactivity.modes.repulse.distance / 6,
          3
        );

        let dx = particuleJs.interactivity.mouse.click_pos_x - p.x,
          dy = particuleJs.interactivity.mouse.click_pos_y - p.y,
          d = dx * dx + dy * dy;

        let force = (-repulseRadius / d) * 1;

        function process() {
          let f = Math.atan2(dy, dx);
          p.vx = force * Math.cos(f);
          p.vy = force * Math.sin(f);

          if (particuleJs.particles.move.out_mode == "bounce") {
            let pos = {
              x: p.x + p.vx,
              y: p.y + p.vy,
            };
            if (pos.x + p.radius > particuleJs.canvas.w) p.vx = -p.vx;
            else if (pos.x - p.radius < 0) p.vx = -p.vx;
            if (pos.y + p.radius > particuleJs.canvas.h) p.vy = -p.vy;
            else if (pos.y - p.radius < 0) p.vy = -p.vy;
          }
        }

        // default
        if (d <= repulseRadius) {
          process();
        }

        // bang - slow motion mode
        // if(!particuleJs.tmp.repulse_finish){
        //   if(d <= repulseRadius){
        //     process();
        //   }
        // }else{
        //   process();
        // }
      } else {
        if (particuleJs.tmp.repulse_clicking == false) {
          p.vx = p.vx_i;
          p.vy = p.vy_i;
        }
      }
    }
  };

  particuleJs.fn.modes.grabParticle = function (p) {
    if (
      particuleJs.interactivity.events.onhover.enable &&
      particuleJs.interactivity.status == "mousemove"
    ) {
      let dx_mouse = p.x - particuleJs.interactivity.mouse.pos_x,
        dy_mouse = p.y - particuleJs.interactivity.mouse.pos_y,
        dist_mouse = Math.sqrt(dx_mouse * dx_mouse + dy_mouse * dy_mouse);

      /* draw a line between the cursor and the particle if the distance between them is under the config distance */
      if (dist_mouse <= particuleJs.interactivity.modes.grab.distance) {
        let opacity_line =
          particuleJs.interactivity.modes.grab.line_linked.opacity -
          dist_mouse /
            (1 / particuleJs.interactivity.modes.grab.line_linked.opacity) /
            particuleJs.interactivity.modes.grab.distance;

        if (opacity_line > 0) {
          /* style */
          let color_line = particuleJs.particles.line_linked.color_rgb_line;
          particuleJs.canvas.ctx.strokeStyle =
            "rgba(" +
            color_line.r +
            "," +
            color_line.g +
            "," +
            color_line.b +
            "," +
            opacity_line +
            ")";
          particuleJs.canvas.ctx.lineWidth =
            particuleJs.particles.line_linked.width;
          //particuleJs.canvas.ctx.lineCap = 'round'; /* performance issue */

          /* path */
          particuleJs.canvas.ctx.beginPath();
          particuleJs.canvas.ctx.moveTo(p.x, p.y);
          particuleJs.canvas.ctx.lineTo(
            particuleJs.interactivity.mouse.pos_x,
            particuleJs.interactivity.mouse.pos_y
          );
          particuleJs.canvas.ctx.stroke();
          particuleJs.canvas.ctx.closePath();
        }
      }
    }
  };

  /* ---------- particuleJs functions - vendors ------------ */

  particuleJs.fn.vendors.eventsListeners = function () {
    /* events target element */
    if (particuleJs.interactivity.detect_on == "window") {
      particuleJs.interactivity.el = window;
    } else {
      particuleJs.interactivity.el = particuleJs.canvas.el;
    }

    /* detect mouse pos - on hover / click event */
    if (
      particuleJs.interactivity.events.onhover.enable ||
      particuleJs.interactivity.events.onclick.enable
    ) {
      /* el on mousemove */
      particuleJs.interactivity.el.addEventListener("mousemove", function (e) {
        if (particuleJs.interactivity.el == window) {
          let pos_x = e.clientX,
            pos_y = e.clientY;
        } else {
          let pos_x = e.offsetX || e.clientX,
            pos_y = e.offsetY || e.clientY;
        }

        particuleJs.interactivity.mouse.pos_x = pos_x;
        particuleJs.interactivity.mouse.pos_y = pos_y;

        if (particuleJs.tmp.retina) {
          particuleJs.interactivity.mouse.pos_x *= particuleJs.canvas.pxratio;
          particuleJs.interactivity.mouse.pos_y *= particuleJs.canvas.pxratio;
        }

        particuleJs.interactivity.status = "mousemove";
      });

      /* el on onmouseleave */
      particuleJs.interactivity.el.addEventListener("mouseleave", function (e) {
        particuleJs.interactivity.mouse.pos_x = null;
        particuleJs.interactivity.mouse.pos_y = null;
        particuleJs.interactivity.status = "mouseleave";
      });
    }

    /* on click event */
    if (particuleJs.interactivity.events.onclick.enable) {
      particuleJs.interactivity.el.addEventListener("click", function () {
        particuleJs.interactivity.mouse.click_pos_x =
          particuleJs.interactivity.mouse.pos_x;
        particuleJs.interactivity.mouse.click_pos_y =
          particuleJs.interactivity.mouse.pos_y;
        particuleJs.interactivity.mouse.click_time = new Date().getTime();

        if (particuleJs.interactivity.events.onclick.enable) {
          switch (particuleJs.interactivity.events.onclick.mode) {
            case "push":
              if (particuleJs.particles.move.enable) {
                particuleJs.fn.modes.pushParticles(
                  particuleJs.interactivity.modes.push.particles_nb,
                  particuleJs.interactivity.mouse
                );
              } else {
                if (particuleJs.interactivity.modes.push.particles_nb == 1) {
                  particuleJs.fn.modes.pushParticles(
                    particuleJs.interactivity.modes.push.particles_nb,
                    particuleJs.interactivity.mouse
                  );
                } else if (
                  particuleJs.interactivity.modes.push.particles_nb > 1
                ) {
                  particuleJs.fn.modes.pushParticles(
                    particuleJs.interactivity.modes.push.particles_nb
                  );
                }
              }
              break;

            case "remove":
              particuleJs.fn.modes.removeParticles(
                particuleJs.interactivity.modes.remove.particles_nb
              );
              break;

            case "bubble":
              particuleJs.tmp.bubble_clicking = true;
              break;

            case "repulse":
              particuleJs.tmp.repulse_clicking = true;
              particuleJs.tmp.repulse_count = 0;
              particuleJs.tmp.repulse_finish = false;
              setTimeout(function () {
                particuleJs.tmp.repulse_clicking = false;
              }, particuleJs.interactivity.modes.repulse.duration * 1000);
              break;
          }
        }
      });
    }
  };

  particuleJs.fn.vendors.densityAutoParticles = function () {
    if (particuleJs.particles.number.density.enable) {
      /* calc area */
      let area =
        (particuleJs.canvas.el.width * particuleJs.canvas.el.height) / 1000;
      if (particuleJs.tmp.retina) {
        area = area / (particuleJs.canvas.pxratio * 2);
      }

      /* calc number of particles based on density area */
      let nb_particles =
        (area * particuleJs.particles.number.value) /
        particuleJs.particles.number.density.value_area;

      /* add or remove X particles */
      let missing_particles = particuleJs.particles.array.length - nb_particles;
      if (missing_particles < 0)
        particuleJs.fn.modes.pushParticles(Math.abs(missing_particles));
      else particuleJs.fn.modes.removeParticles(missing_particles);
    }
  };

  particuleJs.fn.vendors.checkOverlap = function (p1, position) {
    for (let i = 0; i < particuleJs.particles.array.length; i++) {
      let p2 = particuleJs.particles.array[i];

      let dx = p1.x - p2.x,
        dy = p1.y - p2.y,
        dist = Math.sqrt(dx * dx + dy * dy);

      if (dist <= p1.radius + p2.radius) {
        p1.x = position ? position.x : Math.random() * particuleJs.canvas.w;
        p1.y = position ? position.y : Math.random() * particuleJs.canvas.h;
        particuleJs.fn.vendors.checkOverlap(p1);
      }
    }
  };

  particuleJs.fn.vendors.createSvgImg = function (p) {
    /* set color to svg element */
    let svgXml = particuleJs.tmp.source_svg,
      rgbHex = /#([0-9A-F]{3,6})/gi,
      coloredSvgXml = svgXml.replace(rgbHex, function (m, r, g, b) {
        if (p.color.rgb) {
          let color_value =
            "rgba(" +
            p.color.rgb.r +
            "," +
            p.color.rgb.g +
            "," +
            p.color.rgb.b +
            "," +
            p.opacity +
            ")";
        } else {
          let color_value =
            "hsla(" +
            p.color.hsl.h +
            "," +
            p.color.hsl.s +
            "%," +
            p.color.hsl.l +
            "%," +
            p.opacity +
            ")";
        }
        return color_value;
      });

    /* prepare to create img with colored svg */
    let svg = new Blob([coloredSvgXml], {
        type: "image/svg+xml;charset=utf-8",
      }),
      DOMURL = window.URL || window.webkitURL || window,
      url = DOMURL.createObjectURL(svg);

    /* create particle img obj */
    let img = new Image();
    img.addEventListener("load", function () {
      p.img.obj = img;
      p.img.loaded = true;
      DOMURL.revokeObjectURL(url);
      particuleJs.tmp.count_svg++;
    });
    img.src = url;
  };

  particuleJs.fn.vendors.destroyparticuleJs = function () {
    cancelAnimationFrame(particuleJs.fn.drawAnimFrame);
    canvasElements.remove();
    particuleJsDom = null;
  };

  particuleJs.fn.vendors.drawShape = function (
    c,
    startX,
    startY,
    sideLength,
    sideCountNumerator,
    sideCountDenominator
  ) {
    // By Programming Thomas - https://programmingthomas.wordpress.com/2013/04/03/n-sided-shapes/
    let sideCount = sideCountNumerator * sideCountDenominator;
    let decimalSides = sideCountNumerator / sideCountDenominator;
    let interiorAngleDegrees = (180 * (decimalSides - 2)) / decimalSides;
    let interiorAngle = Math.PI - (Math.PI * interiorAngleDegrees) / 180; // convert to radians
    c.save();
    c.beginPath();
    c.translate(startX, startY);
    c.moveTo(0, 0);
    for (let i = 0; i < sideCount; i++) {
      c.lineTo(sideLength, 0);
      c.translate(sideLength, 0);
      c.rotate(interiorAngle);
    }
    //c.stroke();
    c.fill();
    c.restore();
  };

  particuleJs.fn.vendors.exportImg = function () {
    window.open(particuleJs.canvas.el.toDataURL("image/png"), "_blank");
  };

  particuleJs.fn.vendors.loadImg = function (type) {
    particuleJs.tmp.img_error = undefined;

    if (particuleJs.particles.shape.image.src != "") {
      if (type == "svg") {
        let xhr = new XMLHttpRequest();
        xhr.open("GET", particuleJs.particles.shape.image.src);
        xhr.onreadystatechange = function (data) {
          if (xhr.readyState == 4) {
            if (xhr.status == 200) {
              particuleJs.tmp.source_svg = data.currentTarget.response;
              particuleJs.fn.vendors.checkBeforeDraw();
            } else {
              console.log("Error particuleJs - Image not found");
              particuleJs.tmp.img_error = true;
            }
          }
        };
        xhr.send();
      } else {
        let img = new Image();
        img.addEventListener("load", function () {
          particuleJs.tmp.img_obj = img;
          particuleJs.fn.vendors.checkBeforeDraw();
        });
        img.src = particuleJs.particles.shape.image.src;
      }
    } else {
      console.log("Error particuleJs - No image.src");
      particuleJs.tmp.img_error = true;
    }
  };

  particuleJs.fn.vendors.draw = function () {
    if (particuleJs.particles.shape.type == "image") {
      if (particuleJs.tmp.img_type == "svg") {
        if (particuleJs.tmp.count_svg >= particuleJs.particles.number.value) {
          particuleJs.fn.particlesDraw();
          if (!particuleJs.particles.move.enable)
            cancelRequestAnimFrame(particuleJs.fn.drawAnimFrame);
          else
            particuleJs.fn.drawAnimFrame = requestAnimFrame(
              particuleJs.fn.vendors.draw
            );
        } else {
          //console.log('still loading...');
          if (!particuleJs.tmp.img_error)
            particuleJs.fn.drawAnimFrame = requestAnimFrame(
              particuleJs.fn.vendors.draw
            );
        }
      } else {
        if (particuleJs.tmp.img_obj != undefined) {
          particuleJs.fn.particlesDraw();
          if (!particuleJs.particles.move.enable)
            cancelRequestAnimFrame(particuleJs.fn.drawAnimFrame);
          else
            particuleJs.fn.drawAnimFrame = requestAnimFrame(
              particuleJs.fn.vendors.draw
            );
        } else {
          if (!particuleJs.tmp.img_error)
            particuleJs.fn.drawAnimFrame = requestAnimFrame(
              particuleJs.fn.vendors.draw
            );
        }
      }
    } else {
      particuleJs.fn.particlesDraw();
      if (!particuleJs.particles.move.enable)
        cancelRequestAnimFrame(particuleJs.fn.drawAnimFrame);
      else
        particuleJs.fn.drawAnimFrame = requestAnimFrame(
          particuleJs.fn.vendors.draw
        );
    }
  };

  particuleJs.fn.vendors.checkBeforeDraw = function () {
    // if shape is image
    if (particuleJs.particles.shape.type == "image") {
      if (
        particuleJs.tmp.img_type == "svg" &&
        particuleJs.tmp.source_svg == undefined
      ) {
        particuleJs.tmp.checkAnimFrame = requestAnimFrame(check);
      } else {
        //console.log('images loaded! cancel check');
        cancelRequestAnimFrame(particuleJs.tmp.checkAnimFrame);
        if (!particuleJs.tmp.img_error) {
          particuleJs.fn.vendors.init();
          particuleJs.fn.vendors.draw();
        }
      }
    } else {
      particuleJs.fn.vendors.init();
      particuleJs.fn.vendors.draw();
    }
  };

  particuleJs.fn.vendors.init = function () {
    /* init canvas + particles */
    particuleJs.fn.retinaInit();
    particuleJs.fn.canvasInit();
    particuleJs.fn.canvasSize();
    particuleJs.fn.canvasPaint();
    particuleJs.fn.particlesCreate();
    particuleJs.fn.vendors.densityAutoParticles();

    /* particles.line_linked - convert hex colors to rgb */
    particuleJs.particles.line_linked.color_rgb_line = hexToRgb(
      particuleJs.particles.line_linked.color
    );
  };

  particuleJs.fn.vendors.start = function () {
    if (isInArray("image", particuleJs.particles.shape.type)) {
      particuleJs.tmp.img_type = particuleJs.particles.shape.image.src.substr(
        particuleJs.particles.shape.image.src.length - 3
      );
      particuleJs.fn.vendors.loadImg(particuleJs.tmp.img_type);
    } else {
      particuleJs.fn.vendors.checkBeforeDraw();
    }
  };

  /* ---------- particuleJs - start ------------ */

  particuleJs.fn.vendors.eventsListeners();

  particuleJs.fn.vendors.start();
};

/* ---------- global functions - vendors ------------ */

Object.deepExtend = function (destination, source) {
  for (let property in source) {
    if (
      source[property] &&
      source[property].constructor &&
      source[property].constructor === Object
    ) {
      destination[property] = destination[property] || {};
      arguments.callee(destination[property], source[property]);
    } else {
      destination[property] = source[property];
    }
  }
  return destination;
};

window.requestAnimFrame = (function () {
  return (
    window.requestAnimationFrame ||
    window.webkitRequestAnimationFrame ||
    window.mozRequestAnimationFrame ||
    window.oRequestAnimationFrame ||
    window.msRequestAnimationFrame ||
    function (callback) {
      window.setTimeout(callback, 1000 / 60);
    }
  );
})();

window.cancelRequestAnimFrame = (function () {
  return (
    window.cancelAnimationFrame ||
    window.webkitCancelRequestAnimationFrame ||
    window.mozCancelRequestAnimationFrame ||
    window.oCancelRequestAnimationFrame ||
    window.msCancelRequestAnimationFrame ||
    clearTimeout
  );
})();

function hexToRgb(hex) {
  // By Tim Down - http://stackoverflow.com/a/5624139/3493650
  // Expand shorthand form (e.g. "03F") to full form (e.g. "0033FF")
  let shorthandRegex = /^#?([a-f\d])([a-f\d])([a-f\d])$/i;
  hex = hex.replace(shorthandRegex, function (m, r, g, b) {
    return r + r + g + g + b + b;
  });
  let result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
  return result
    ? {
        r: parseInt(result[1], 16),
        g: parseInt(result[2], 16),
        b: parseInt(result[3], 16),
      }
    : null;
}

function clamp(number, min, max) {
  return Math.min(Math.max(number, min), max);
}

function isInArray(value, array) {
  return array.indexOf(value) > -1;
}

/* ---------- particles.js functions - start ------------ */

window.particuleJsDom = [];

window.particlesJS = function (tag_id, params) {
  //console.log(params);

  /* no string id? so it's object params, and set the id with default id */
  if (typeof tag_id != "string") {
    params = tag_id;
    tag_id = "particles-js";
  }

  /* no id? set the id to default id */
  if (!tag_id) {
    tag_id = "particles-js";
  }

  /* particuleJs elements */
  let particuleJs_tag = document.getElementById(tag_id),
    particuleJs_canvas_class = "particles-js-canvas-el",
    exist_canvas = particuleJs_tag.getElementsByClassName(
      particuleJs_canvas_class
    );

  /* remove canvas if exists into the particuleJs target tag */
  if (exist_canvas.length) {
    while (exist_canvas.length > 0) {
      particuleJs_tag.removeChild(exist_canvas[0]);
    }
  }

  /* create canvas element */
  let canvasElements = document.createElement("canvas");
  canvasElements.className = particuleJs_canvas_class;

  /* set size canvas */
  canvasElements.style.width = "100%";
  canvasElements.style.height = "100%";

  /* append canvas */
  let canvas = document.getElementById(tag_id).appendChild(canvasElements);

  /* launch particle.js */
  if (canvas != null) {
    particuleJsDom.push(new particuleJs(tag_id, params));
  }
};

window.particlesJS.load = function (tag_id, path_config_json, callback) {
  /* load json config */
  let xhr = new XMLHttpRequest();
  xhr.open("GET", path_config_json);
  xhr.onreadystatechange = function (data) {
    if (xhr.readyState == 4) {
      if (xhr.status == 200) {
        let params = JSON.parse(data.currentTarget.response);
        window.particlesJS(tag_id, params);
        if (callback) callback();
      } else {
        console.log("Error particuleJs - XMLHttpRequest status: " + xhr.status);
        console.log("Error particuleJs - File config not found");
      }
    }
  };
  xhr.send();
};
